// Central app configuration
const APP_CONFIG = {
  IS_PREMIUM: false,

  // Functional limits for inputs and runtime tasks.
  LIMITS: {
    maxLikes: 10000,
    maxComments: 10000,
    maxFollow: 10000,
    runtimeFreeLimit: 10000000
  },
};
